import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class LineBarforCategoryBar extends StatelessWidget {
  const LineBarforCategoryBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 15.0,
      width: 500.0,
      child: Divider(
        color: Colors.black,
      ),
    );
  }
}
